#include <stdio.h>
#include <stdlib.h>
#include <string.h>

#include "../include/sort_dependency.h"
#include "../include/map_dependency.h"

void sort_dependencies(MapDependency *map) {
    /* 
        Sort the dependencies in the map
        Use a topological sort algorithm to ensure that dependencies are resolved
        before their dependents
    */
}